<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="ko"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://wonjun-s-dev.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://wonjun-s-dev.github.io/" rel="alternate" type="text/html" hreflang="ko"/><updated>2026-07-10T12:53:35+00:00</updated><id>https://wonjun-s-dev.github.io/feed.xml</id><title type="html">WonJun’s Blog</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">iPad Homepage 프로젝트에서 생각해봐야할 5가지 (1)</title><link href="https://wonjun-s-dev.github.io/blog/2026/01-post/" rel="alternate" type="text/html" title="iPad Homepage 프로젝트에서 생각해봐야할 5가지 (1)"/><published>2026-07-10T00:00:00+00:00</published><updated>2026-07-10T00:00:00+00:00</updated><id>https://wonjun-s-dev.github.io/blog/2026/01-post</id><content type="html" xml:base="https://wonjun-s-dev.github.io/blog/2026/01-post/"><![CDATA[<div style="word-break: keep-all; overflow-wrap: break-word;"> <blockquote> <p><strong>SOURCE</strong> <code class="language-plaintext highlighter-rouge">ipad-homepage</code> (fastcampus clone) · <strong>STACK</strong> vanilla HTML/CSS/JS · <strong>다룰 기법</strong> 5가지</p> </blockquote> <p>프레임워크 없이 애플 홈페이지를 그대로 흉내 낸 학습용 클론 저장소를 코드 리뷰하듯 훑었다. 라이브러리 하나 없이도 애플 특유의 인터랙션을 만들어낸 지점 다섯 곳을, 코드 + “왜 이렇게 짰을까”에 대한 심화 분석과 함께 정리한다.</p> <h2 id="steps1----">1. 스프라이트시트 + <code class="language-plaintext highlighter-rouge">steps(1)</code>로 만든 무한 루프 아이콘</h2> <p>정보 블록(<code class="language-plaintext highlighter-rouge">.info</code>) 옆에 붙는 17개 아이콘은 전부 같은 <code class="language-plaintext highlighter-rouge">@keyframes sprite-icon</code> 하나를 공유한다. 다른 건 <code class="language-plaintext highlighter-rouge">background-image</code>뿐이다. 각 아이콘은 6×10 격자로 그려진 60프레임짜리 PNG이고, 키프레임은 1.67%(=1/60)마다 <code class="language-plaintext highlighter-rouge">background-position</code>을 한 칸씩 옮긴다.</p> <div class="language-css highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">.info</span> <span class="nc">.icon</span> <span class="p">{</span>
  <span class="nl">width</span><span class="p">:</span> <span class="m">100px</span><span class="p">;</span>
  <span class="nl">height</span><span class="p">:</span> <span class="m">100px</span><span class="p">;</span>
  <span class="nl">animation</span><span class="p">:</span> <span class="n">sprite-icon</span> <span class="m">3s</span> <span class="nf">steps</span><span class="p">(</span><span class="m">1</span><span class="p">)</span> <span class="nb">infinite</span><span class="p">;</span>
<span class="p">}</span>
<span class="nc">.info</span> <span class="nc">.icon.icon--battery</span> <span class="p">{</span>
  <span class="nl">background-image</span><span class="p">:</span> <span class="nf">url</span><span class="p">(</span><span class="s2">'../images/sprite_battery.png'</span><span class="p">);</span>
<span class="p">}</span>

<span class="k">@keyframes</span> <span class="n">sprite-icon</span> <span class="p">{</span>
  <span class="err">0</span><span class="o">.</span><span class="m">00%</span>  <span class="p">{</span> <span class="nl">background-position</span><span class="p">:</span> <span class="m">0</span> <span class="m">0</span><span class="p">;</span> <span class="p">}</span>
  <span class="err">1</span><span class="o">.</span><span class="m">67%</span>  <span class="p">{</span> <span class="nl">background-position</span><span class="p">:</span> <span class="m">-100px</span> <span class="m">0</span><span class="p">;</span> <span class="p">}</span>
  <span class="err">3</span><span class="o">.</span><span class="m">33%</span>  <span class="p">{</span> <span class="nl">background-position</span><span class="p">:</span> <span class="m">-200px</span> <span class="m">0</span><span class="p">;</span> <span class="p">}</span>
  <span class="c">/* … 10%마다 줄이 바뀌며 총 60프레임 */</span>
<span class="p">}</span>
</code></pre></div> </div> <p><code class="language-plaintext highlighter-rouge">css/main.css:95–100, 1062–</code></p> <h3 id="stepsn----">왜 <code class="language-plaintext highlighter-rouge">steps(N)</code> 한 줄로 안 끝냈을까</h3> <p>프레임이 가로로 한 줄뿐이면 <code class="language-plaintext highlighter-rouge">steps(6)</code> 하나로 충분하다. 하지만 실제 스프라이트는 6열 10행 격자라 10프레임마다 <code class="language-plaintext highlighter-rouge">background-position</code>의 y값도 같이 바뀌어야 한다. CSS는 스텝 함수 안에서 “6번마다 줄바꿈” 같은 나머지 연산을 못 하기 때문에, 60개 퍼센트 지점을 전부 손으로 나열해 x·y를 동시에 지정한 것이다.</p> <h3 id="steps1-vs-steps60----">심화 — <code class="language-plaintext highlighter-rouge">steps(1)</code> vs <code class="language-plaintext highlighter-rouge">steps(60)</code>, 그리고 왜 성능이 괜찮은가</h3> <p><code class="language-plaintext highlighter-rouge">animation-timing-function</code>이 기본값(<code class="language-plaintext highlighter-rouge">ease</code>)이었다면 브라우저가 프레임 사이 값을 보간(interpolation)하려고 시도하면서 <code class="language-plaintext highlighter-rouge">background-position</code>이 미세하게 계속 움직여 어색하게 번진다. <code class="language-plaintext highlighter-rouge">steps(1)</code>은 각 구간 시작 시점에 딱 한 번만 값을 스냅(snap)시키므로, 60개의 정지 이미지를 순서대로 갈아 끼우는 것과 동일한 결과가 된다. <code class="language-plaintext highlighter-rouge">steps(60, jump-none)</code>처럼 스텝 개수를 명시하는 방법도 있지만, 이 경우 키프레임 offset과 스텝 개수가 정확히 일치해야 해서 관리 포인트가 늘어난다. <code class="language-plaintext highlighter-rouge">steps(1)</code> + 60개의 개별 offset 조합이 오히려 “각 프레임마다 임의의 좌표를 지정할 수 있다”는 유연성을 준다.</p> <p>성능 측면에서는 <code class="language-plaintext highlighter-rouge">background-position</code> 애니메이션이 페인트(paint) 단계를 매 프레임 다시 태우는 비교적 무거운 속성이다. <code class="language-plaintext highlighter-rouge">transform</code>처럼 컴포지터 스레드에서 처리되지 않기 때문에, 아이콘 개수가 많아지면 (여기선 17개) 메인 스레드 부담이 누적된다. 실무에서 아이콘 수가 더 많아진다면 <code class="language-plaintext highlighter-rouge">will-change: background-position</code>을 걸어 레이어를 분리하거나, 아예 스프라이트를 <code class="language-plaintext highlighter-rouge">&lt;canvas&gt;</code> + <code class="language-plaintext highlighter-rouge">requestAnimationFrame</code>으로 그리는 방식으로 옮기는 게 낫다. SCSS를 쓴다면 60줄을 손으로 나열하는 대신 <code class="language-plaintext highlighter-rouge">@for</code> 루프로 키프레임을 생성해 실수를 줄일 수 있다.</p> <div class="language-scss highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">@keyframes</span> <span class="nt">sprite-icon</span> <span class="p">{</span>
  <span class="k">@for</span> <span class="nv">$i</span> <span class="ow">from</span> <span class="m">0</span> <span class="ow">through</span> <span class="m">59</span> <span class="p">{</span>
    <span class="si">#{</span><span class="nv">$i</span> <span class="o">*</span> <span class="p">(</span><span class="m">100%</span> <span class="o">/</span> <span class="m">60</span><span class="p">)</span><span class="si">}</span> <span class="p">{</span>
      <span class="nv">$col</span><span class="p">:</span> <span class="nv">$i</span> <span class="o">%</span> <span class="m">6</span><span class="p">;</span>
      <span class="nv">$row</span><span class="p">:</span> <span class="nf">floor</span><span class="p">(</span><span class="nv">$i</span> <span class="o">/</span> <span class="m">6</span><span class="p">);</span>
      <span class="nl">background-position</span><span class="p">:</span> <span class="o">-</span><span class="si">#{</span><span class="nv">$col</span> <span class="o">*</span> <span class="m">100px</span><span class="si">}</span> <span class="o">-</span><span class="si">#{</span><span class="nv">$row</span> <span class="o">*</span> <span class="m">100px</span><span class="si">}</span><span class="p">;</span>
    <span class="p">}</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div> </div> <hr/> <h2 id="intersectionobserver----css-">2. IntersectionObserver는 클래스만 토글하고, 애니메이션은 CSS가 전담</h2> <p>스크롤 리빌 로직은 관찰자 하나로 모든 <code class="language-plaintext highlighter-rouge">.info</code> 요소를 감시한다. 뷰포트에 들어오면 하는 일은 딱 하나, <code class="language-plaintext highlighter-rouge">.show</code> 클래스를 붙이는 것뿐이다. 실제 움직임(<code class="language-plaintext highlighter-rouge">translate</code>, <code class="language-plaintext highlighter-rouge">opacity</code>)은 CSS <code class="language-plaintext highlighter-rouge">transition</code>이 담당한다.</p> <div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">io</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">IntersectionObserver</span><span class="p">(</span><span class="nf">function </span><span class="p">(</span><span class="nx">entries</span><span class="p">)</span> <span class="p">{</span>
  <span class="nx">entries</span><span class="p">.</span><span class="nf">forEach</span><span class="p">(</span><span class="nf">function </span><span class="p">(</span><span class="nx">entry</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if </span><span class="p">(</span><span class="o">!</span><span class="nx">entry</span><span class="p">.</span><span class="nx">isIntersecting</span><span class="p">)</span> <span class="k">return</span><span class="p">;</span>
    <span class="nx">entry</span><span class="p">.</span><span class="nx">target</span><span class="p">.</span><span class="nx">classList</span><span class="p">.</span><span class="nf">add</span><span class="p">(</span><span class="dl">'</span><span class="s1">show</span><span class="dl">'</span><span class="p">);</span>
  <span class="p">});</span>
<span class="p">});</span>
<span class="nx">infoEls</span><span class="p">.</span><span class="nf">forEach</span><span class="p">(</span><span class="nf">function </span><span class="p">(</span><span class="nx">el</span><span class="p">)</span> <span class="p">{</span> <span class="nx">io</span><span class="p">.</span><span class="nf">observe</span><span class="p">(</span><span class="nx">el</span><span class="p">);</span> <span class="p">});</span>
</code></pre></div> </div> <div class="language-css highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">.info</span> <span class="p">{</span>
  <span class="nl">transform</span><span class="p">:</span> <span class="nf">translate</span><span class="p">(</span><span class="m">0</span><span class="p">,</span> <span class="m">100px</span><span class="p">);</span>
  <span class="nl">opacity</span><span class="p">:</span> <span class="m">0</span><span class="p">;</span>
  <span class="nl">transition</span><span class="p">:</span> <span class="m">1s</span><span class="p">;</span>
<span class="p">}</span>
<span class="nc">.info.show</span> <span class="p">{</span>
  <span class="nl">transform</span><span class="p">:</span> <span class="nf">translate</span><span class="p">(</span><span class="m">0</span><span class="p">,</span> <span class="m">0</span><span class="p">);</span>
  <span class="nl">opacity</span><span class="p">:</span> <span class="m">1</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div> </div> <p><code class="language-plaintext highlighter-rouge">js/main.js:73–84</code>, <code class="language-plaintext highlighter-rouge">css/main.css:81–91</code></p> <h3 id="section">역할 분리가 왜 “깔끔한” 설계인가</h3> <p>JS는 “봤다/안 봤다”라는 <strong>상태 판단</strong>만 하고, “어떻게 움직일지”라는 <strong>표현(presentation)</strong>은 CSS에 위임했다. 이 경계를 지키면 애니메이션 곡선이나 지속시간을 바꿀 때 JS를 건드릴 필요가 없고, 반대로 트리거 조건(threshold, rootMargin)을 바꿀 때 CSS를 건드릴 필요가 없다. <code class="language-plaintext highlighter-rouge">scroll</code> 이벤트 + <code class="language-plaintext highlighter-rouge">getBoundingClientRect()</code>로 같은 걸 구현했다면 매 스크롤마다 강제 레이아웃(forced reflow)이 발생했겠지만, <code class="language-plaintext highlighter-rouge">IntersectionObserver</code>는 브라우저가 컴포지터 단계에서 비동기로 교차 여부를 계산해주므로 메인 스레드 부담이 훨씬 적다.</p> <h3 id="unobserve-">아쉬운 점 1 — <code class="language-plaintext highlighter-rouge">unobserve</code> 누락</h3> <p>한 번 <code class="language-plaintext highlighter-rouge">show</code>가 붙은 뒤에도 <code class="language-plaintext highlighter-rouge">observer.unobserve(entry.target)</code>을 호출하지 않는다. 요소가 화면을 스쳐 지날 때마다 콜백은 계속 실행되고, 매번 <code class="language-plaintext highlighter-rouge">classList.add</code>가 (이미 있는 클래스에) 헛수고로 호출된다. 리빌 애니메이션은 한 번 재생되면 끝이라 동작은 같지만, 콜백 호출 자체는 줄일 수 있다.</p> <div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">io</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">IntersectionObserver</span><span class="p">(</span><span class="nf">function </span><span class="p">(</span><span class="nx">entries</span><span class="p">,</span> <span class="nx">observer</span><span class="p">)</span> <span class="p">{</span>
  <span class="nx">entries</span><span class="p">.</span><span class="nf">forEach</span><span class="p">(</span><span class="nf">function </span><span class="p">(</span><span class="nx">entry</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if </span><span class="p">(</span><span class="o">!</span><span class="nx">entry</span><span class="p">.</span><span class="nx">isIntersecting</span><span class="p">)</span> <span class="k">return</span><span class="p">;</span>
    <span class="nx">entry</span><span class="p">.</span><span class="nx">target</span><span class="p">.</span><span class="nx">classList</span><span class="p">.</span><span class="nf">add</span><span class="p">(</span><span class="dl">'</span><span class="s1">show</span><span class="dl">'</span><span class="p">);</span>
    <span class="nx">observer</span><span class="p">.</span><span class="nf">unobserve</span><span class="p">(</span><span class="nx">entry</span><span class="p">.</span><span class="nx">target</span><span class="p">);</span> <span class="c1">// 한 번 트리거되면 관찰 종료</span>
  <span class="p">});</span>
<span class="p">});</span>
</code></pre></div> </div> <h3 id="thresholdrootmargin---">아쉬운 점 2 — threshold/rootMargin 기본값 그대로 사용</h3> <p>옵션 없이 생성했기 때문에 기본 <code class="language-plaintext highlighter-rouge">threshold: 0</code>, <code class="language-plaintext highlighter-rouge">root: null</code>(뷰포트)이 적용된다. 즉 요소가 <strong>1px만 걸쳐도</strong> 콜백이 발동한다. 애플 실제 사이트는 요소가 화면에 어느 정도(대략 20~30%) 들어와야 리빌이 시작되는 느낌을 준다. <code class="language-plaintext highlighter-rouge">rootMargin: '0px 0px -15% 0px'</code> 정도로 뷰포트 하단을 안쪽으로 당겨주면, 스크롤하다 화면 맨 아래에 살짝 걸친 요소가 바로 튀어나오는 어색함을 줄일 수 있다.</p> <hr/> <h2 id="reverse----">3. 배열 <code class="language-plaintext highlighter-rouge">reverse()</code> 하나로 만든 스태거 애니메이션</h2> <p>검색창을 열 때 헤더 메뉴와 자동완성 항목이 순서대로 나타나고, 닫을 때는 반대 순서로 사라진다. 비결은 <code class="language-plaintext highlighter-rouge">transitionDelay = (index * 0.4) / length</code>로 지연시간을 계산해두고, 열고 닫을 때마다 대상 배열을 <code class="language-plaintext highlighter-rouge">reverse()</code>로 뒤집는 것뿐이다.</p> <div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">function</span> <span class="nf">showSearch</span><span class="p">()</span> <span class="p">{</span>
  <span class="nx">headerMenuEls</span><span class="p">.</span><span class="nf">reverse</span><span class="p">().</span><span class="nf">forEach</span><span class="p">(</span><span class="nf">function </span><span class="p">(</span><span class="nx">el</span><span class="p">,</span> <span class="nx">index</span><span class="p">)</span> <span class="p">{</span>
    <span class="nx">el</span><span class="p">.</span><span class="nx">style</span><span class="p">.</span><span class="nx">transitionDelay</span> <span class="o">=</span> <span class="p">(</span><span class="nx">index</span> <span class="o">*</span> <span class="mf">0.4</span><span class="p">)</span> <span class="o">/</span> <span class="nx">headerMenuEls</span><span class="p">.</span><span class="nx">length</span> <span class="o">+</span> <span class="dl">'</span><span class="s1">s</span><span class="dl">'</span><span class="p">;</span>
  <span class="p">});</span>
  <span class="c1">// ...</span>
<span class="p">}</span>
<span class="kd">function</span> <span class="nf">hideSearch</span><span class="p">()</span> <span class="p">{</span>
  <span class="nx">headerMenuEls</span><span class="p">.</span><span class="nf">reverse</span><span class="p">().</span><span class="nf">forEach</span><span class="p">(</span><span class="nf">function </span><span class="p">(</span><span class="nx">el</span><span class="p">,</span> <span class="nx">index</span><span class="p">)</span> <span class="p">{</span>
    <span class="nx">el</span><span class="p">.</span><span class="nx">style</span><span class="p">.</span><span class="nx">transitionDelay</span> <span class="o">=</span> <span class="p">(</span><span class="nx">index</span> <span class="o">*</span> <span class="mf">0.4</span><span class="p">)</span> <span class="o">/</span> <span class="nx">headerMenuEls</span><span class="p">.</span><span class="nx">length</span> <span class="o">+</span> <span class="dl">'</span><span class="s1">s</span><span class="dl">'</span><span class="p">;</span>
  <span class="p">});</span>
  <span class="c1">// 같은 배열을 다시 reverse() → 순서가 뒤집혀 역순으로 재생</span>
<span class="p">}</span>
</code></pre></div> </div> <p><code class="language-plaintext highlighter-rouge">js/main.js:46–70</code></p> <h3 id="arrayprototypereverse--">심화 — <code class="language-plaintext highlighter-rouge">Array.prototype.reverse()</code>는 원본을 파괴한다</h3> <p>여기서 핵심은 <code class="language-plaintext highlighter-rouge">reverse()</code>가 <strong>새 배열을 반환하는 게 아니라 원본 배열 자체를 뒤집고 그 참조를 반환</strong>한다는 점이다(<code class="language-plaintext highlighter-rouge">sort()</code>도 동일). 그래서 <code class="language-plaintext highlighter-rouge">showSearch → hideSearch → showSearch → ...</code> 순으로 계속 토글하면 <code class="language-plaintext highlighter-rouge">headerMenuEls</code>는 호출될 때마다 순서가 반전되고, 짝수 번째 호출에서는 원래 순서, 홀수 번째 호출에서는 역순이 된다. 코드가 “열 때 A→B→C 순, 닫을 때 C→B→A 순”으로 항상 동작하는 것처럼 보이는 이유가 바로 이 부수효과(side effect)를 정확히 이용했기 때문이다.</p> <p>다만 이건 양날의 검이다. 만약 두 함수 중 하나가 예외를 던지고 중간에 리턴하는 상황이 생기거나, 다른 코드가 <code class="language-plaintext highlighter-rouge">headerMenuEls</code>를 참조해서 순서를 가정하고 있다면 이 암묵적 상태 변화 때문에 버그가 생기기 쉽다. 의도가 명확하게 드러나도록 짜려면 원본을 건드리지 않는 편이 안전하다.</p> <div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 원본 순서를 유지하면서 열고 닫을 때 방향만 명시적으로 결정</span>
<span class="kd">function</span> <span class="nf">animateStagger</span><span class="p">(</span><span class="nx">els</span><span class="p">,</span> <span class="nx">direction</span><span class="p">)</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">ordered</span> <span class="o">=</span> <span class="nx">direction</span> <span class="o">===</span> <span class="dl">'</span><span class="s1">open</span><span class="dl">'</span> <span class="p">?</span> <span class="nx">els</span> <span class="p">:</span> <span class="p">[...</span><span class="nx">els</span><span class="p">].</span><span class="nf">reverse</span><span class="p">();</span>
  <span class="nx">ordered</span><span class="p">.</span><span class="nf">forEach</span><span class="p">(</span><span class="nf">function </span><span class="p">(</span><span class="nx">el</span><span class="p">,</span> <span class="nx">index</span><span class="p">)</span> <span class="p">{</span>
    <span class="nx">el</span><span class="p">.</span><span class="nx">style</span><span class="p">.</span><span class="nx">transitionDelay</span> <span class="o">=</span> <span class="p">(</span><span class="nx">index</span> <span class="o">*</span> <span class="mf">0.4</span><span class="p">)</span> <span class="o">/</span> <span class="nx">ordered</span><span class="p">.</span><span class="nx">length</span> <span class="o">+</span> <span class="dl">'</span><span class="s1">s</span><span class="dl">'</span><span class="p">;</span>
  <span class="p">});</span>
<span class="p">}</span>
</code></pre></div> </div> <p>스프레드(<code class="language-plaintext highlighter-rouge">[...els]</code>)로 얕은 복사를 뜨고 <code class="language-plaintext highlighter-rouge">reverse()</code>를 적용하면, 원본 배열의 순서는 항상 고정되고 “이번 호출이 open인지 close인지”만으로 동작이 결정되어 호출 횟수(홀/짝)에 의존하지 않는 안전한 구조가 된다.</p> <hr/> <h2 id="mask-image----">4. <code class="language-plaintext highlighter-rouge">mask-image</code>로 비디오 가장자리를 부드럽게 잘라내기</h2> <p>카메라 섹션의 <code class="language-plaintext highlighter-rouge">&lt;video&gt;</code> 태그에는 <code class="language-plaintext highlighter-rouge">-webkit-mask-image</code>로 PNG 마스크가 씌워져 있다.</p> <div class="language-css highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">.camera</span> <span class="nc">.stage</span> <span class="nt">video</span> <span class="p">{</span>
  <span class="nl">position</span><span class="p">:</span> <span class="nb">absolute</span><span class="p">;</span>
  <span class="nl">top</span><span class="p">:</span> <span class="m">36px</span><span class="p">;</span>
  <span class="nl">left</span><span class="p">:</span> <span class="m">76px</span><span class="p">;</span>
  <span class="nl">-webkit-mask-image</span><span class="p">:</span> <span class="nf">url</span><span class="p">(</span><span class="s2">'../images/camera_video_mask.png'</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div> </div> <p><code class="language-plaintext highlighter-rouge">css/main.css:676–680</code></p> <h3 id="clip-path--mask-image-"><code class="language-plaintext highlighter-rouge">clip-path</code>가 아니라 <code class="language-plaintext highlighter-rouge">mask-image</code>인 이유</h3> <p><code class="language-plaintext highlighter-rouge">clip-path</code>를 두고 굳이 마스크 이미지를 쓴 이유는 모양이 아니라 <strong>경계의 품질</strong> 때문이다. <code class="language-plaintext highlighter-rouge">clip-path</code>는 도형이든 SVG든 잘라낸 가장자리가 항상 칼같이 날카롭다(anti-aliasing이 약간 있어도 그라디언트 페더링은 불가능). 반면 <code class="language-plaintext highlighter-rouge">mask-image</code>는 마스크 이미지의 흑백(알파) 그라디언트를 그대로 투명도로 옮기기 때문에, 페더링된 부드러운 가장자리를 그대로 재현할 수 있다.</p> <h3 id="section-1">심화 — 실무에서 놓치기 쉬운 부분</h3> <p>이 코드에는 <code class="language-plaintext highlighter-rouge">mask-size</code>, <code class="language-plaintext highlighter-rouge">mask-repeat</code>, <code class="language-plaintext highlighter-rouge">mask-position</code>이 생략돼 있다. 기본값(<code class="language-plaintext highlighter-rouge">mask-size: auto</code>, <code class="language-plaintext highlighter-rouge">mask-repeat: repeat</code>)이 마스크 PNG의 실제 크기·비율과 우연히 맞아떨어져서 동작하는 것으로 보이는데, 비디오 요소 크기가 반응형으로 바뀌는 레이아웃이었다면 마스크가 타일링되거나 어긋났을 가능성이 높다. 프로덕션에서 이 패턴을 쓸 땐 아래처럼 명시하는 게 안전하다.</p> <div class="language-css highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">.camera</span> <span class="nc">.stage</span> <span class="nt">video</span> <span class="p">{</span>
  <span class="nl">-webkit-mask-image</span><span class="p">:</span> <span class="nf">url</span><span class="p">(</span><span class="s2">'../images/camera_video_mask.png'</span><span class="p">);</span>
  <span class="nl">-webkit-mask-size</span><span class="p">:</span> <span class="m">100%</span> <span class="m">100%</span><span class="p">;</span>
  <span class="nl">-webkit-mask-repeat</span><span class="p">:</span> <span class="nb">no-repeat</span><span class="p">;</span>
  <span class="nl">mask-image</span><span class="p">:</span> <span class="nf">url</span><span class="p">(</span><span class="s2">'../images/camera_video_mask.png'</span><span class="p">);</span>
  <span class="nl">mask-size</span><span class="p">:</span> <span class="m">100%</span> <span class="m">100%</span><span class="p">;</span>
  <span class="nl">mask-repeat</span><span class="p">:</span> <span class="nb">no-repeat</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div> </div> <p>또한 <code class="language-plaintext highlighter-rouge">-webkit-</code> 접두사만 있고 표준 <code class="language-plaintext highlighter-rouge">mask-image</code>가 없는데, Safari는 여전히 접두사 버전이 필요하지만 Chrome/Edge(Chromium 계열)는 이미 오래전부터 비접두사 <code class="language-plaintext highlighter-rouge">mask-image</code>를 지원한다. 크로스 브라우저 대응이 필요하면 두 버전을 함께 선언해야 한다. 성능 면에서는 <code class="language-plaintext highlighter-rouge">mask-image</code>가 적용된 요소는 별도 컴포지팅 레이어로 승격되는데, <code class="language-plaintext highlighter-rouge">&lt;video&gt;</code>처럼 이미 무거운 요소에 마스크를 씌우면 스크롤 중 프레임 드랍이 발생할 수 있어 모바일에서는 마스크 크기를 필요 이상으로 키우지 않는 게 좋다.</p> <hr/> <h2 id="section-2">5. 이미지 도용 방지, 세 줄이면 충분하다 (단, 한계는 명확히)</h2> <p>전역 <code class="language-plaintext highlighter-rouge">img</code> 셀렉터에 걸린 세 줄은 실무에서 자주 나오는 “이미지 우클릭 저장/드래그 방지” 요구사항의 가장 짧은 해법이다.</p> <div class="language-css highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">img</span> <span class="p">{</span>
  <span class="nl">display</span><span class="p">:</span> <span class="nb">block</span><span class="p">;</span>
  <span class="py">user-select</span><span class="p">:</span> <span class="nb">none</span><span class="p">;</span>
  <span class="nl">-webkit-user-drag</span><span class="p">:</span> <span class="nb">none</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div> </div> <p><code class="language-plaintext highlighter-rouge">css/main.css:33–37</code></p> <p><code class="language-plaintext highlighter-rouge">user-select: none</code>은 드래그로 이미지가 텍스트처럼 선택되는 것을 막고, <code class="language-plaintext highlighter-rouge">-webkit-user-drag: none</code>은 이미지를 끌어 데스크톱이나 다른 탭에 놓는 것을 막는다. 끼워 넣은 <code class="language-plaintext highlighter-rouge">display: block</code>은 보안과 무관하게, <code class="language-plaintext highlighter-rouge">img</code>가 인라인 요소일 때 하단에 생기는 미세한 여백(baseline gap)을 없애는 덤이다.</p> <h3 id="section-3">심화 — 이건 “방지”가 아니라 “억제”다</h3> <p>세 줄로 막을 수 있는 건 <strong>마우스 드래그</strong>와 <strong>더블클릭 텍스트 선택</strong> 정도의 캐주얼한 시도뿐이다. 개발자 도구 Network 탭, <code class="language-plaintext highlighter-rouge">curl</code>, 우클릭 컨텍스트 메뉴의 “이미지 저장”(<code class="language-plaintext highlighter-rouge">contextmenu</code> 이벤트를 별도로 막지 않는 한 여전히 동작), 스크린샷은 전혀 막지 못한다. 실제로 이미지 자체를 서버에서 응답으로 내려주는 이상 클라이언트 사이드에서 완벽히 막는 방법은 존재하지 않는다. 좀 더 방어를 강화하려면 아래를 함께 고려한다.</p> <ul> <li><code class="language-plaintext highlighter-rouge">contextmenu</code> 이벤트에 <code class="language-plaintext highlighter-rouge">preventDefault()</code>를 걸어 우클릭 메뉴 자체를 차단 (단, 접근성/사용성 저하 트레이드오프)</li> <li>원본 대신 워터마크가 박힌 이미지를 제공하거나, CSS <code class="language-plaintext highlighter-rouge">background-image</code>로 감춰 <code class="language-plaintext highlighter-rouge">&lt;img&gt;</code> 태그 저장을 우회적으로 어렵게 만듦</li> <li>진짜 민감한 자산이라면 서명된 URL(signed URL) + 짧은 만료시간으로 서버 단에서 접근을 통제 (클라이언트 트릭보다 훨씬 신뢰할 수 있는 방어선)</li> </ul> <p>결국 CSS 세 줄은 “일반 사용자의 무심한 드래그 저장”을 막는 UX적 장치이지, 보안 대책은 아니라는 걸 팀 내에서 명확히 공유해두는 게 중요하다.</p> <hr/> <h2 id="section-4">정리 표</h2> <table> <thead> <tr> <th>#</th> <th>기법</th> <th>핵심 API/속성</th> <th>강점</th> <th>실무 적용 시 주의점</th> </tr> </thead> <tbody> <tr> <td>1</td> <td>스프라이트 아이콘 애니메이션</td> <td><code class="language-plaintext highlighter-rouge">steps(1)</code>, <code class="language-plaintext highlighter-rouge">background-position</code></td> <td>JS 없이 GIF 느낌 재현</td> <td><code class="language-plaintext highlighter-rouge">background-position</code> 애니메이션은 페인트 비용 큼, 아이콘 많으면 canvas 고려</td> </tr> <tr> <td>2</td> <td>스크롤 리빌</td> <td><code class="language-plaintext highlighter-rouge">IntersectionObserver</code>, class 토글</td> <td>JS/CSS 역할 분리</td> <td><code class="language-plaintext highlighter-rouge">unobserve</code> 누락 시 콜백 낭비, threshold/rootMargin 튜닝 필요</td> </tr> <tr> <td>3</td> <td>스태거 애니메이션</td> <td><code class="language-plaintext highlighter-rouge">transitionDelay</code>, <code class="language-plaintext highlighter-rouge">Array.reverse()</code></td> <td>코드 3줄로 파도 효과</td> <td><code class="language-plaintext highlighter-rouge">reverse()</code>는 원본 파괴, 호출 횟수 의존성 주의</td> </tr> <tr> <td>4</td> <td>비디오 마스킹</td> <td><code class="language-plaintext highlighter-rouge">mask-image</code></td> <td><code class="language-plaintext highlighter-rouge">clip-path</code> 대비 부드러운 경계</td> <td><code class="language-plaintext highlighter-rouge">mask-size</code>/<code class="language-plaintext highlighter-rouge">repeat</code> 명시 필요, 컴포지팅 비용</td> </tr> <tr> <td>5</td> <td>이미지 저장 방지</td> <td><code class="language-plaintext highlighter-rouge">user-select</code>, <code class="language-plaintext highlighter-rouge">user-drag</code></td> <td>최소 코드로 캐주얼 저장 차단</td> <td>실질적 보안 대책 아님, 진짜 보호는 서버 단에서</td> </tr> </tbody> </table> <hr/> <h3 id="typescript--">TypeScript 프로젝트에서의 참고</h3> <p>이 다섯 기법을 React/Next.js + TypeScript 스택으로 옮긴다면 다음 지점만 신경 쓰면 된다.</p> <ul> <li><strong>스프라이트 애니메이션</strong>: <code class="language-plaintext highlighter-rouge">@keyframes</code>를 SCSS <code class="language-plaintext highlighter-rouge">@for</code>나 styled-components의 <code class="language-plaintext highlighter-rouge">css</code> 헬퍼로 생성하고, 아이콘 이름·프레임 수를 <code class="language-plaintext highlighter-rouge">Record&lt;IconName, { frames: number; sheet: string }&gt;</code> 같은 타입으로 관리하면 아이콘 추가 시 실수를 컴파일 타임에 잡을 수 있다.</li> <li><strong>IntersectionObserver</strong>: <code class="language-plaintext highlighter-rouge">useEffect</code> 안에서 생성하고 클린업에서 <code class="language-plaintext highlighter-rouge">observer.disconnect()</code>를 호출하는 커스텀 훅(<code class="language-plaintext highlighter-rouge">useInView</code> 류)으로 감싸면 위에서 지적한 <code class="language-plaintext highlighter-rouge">unobserve</code> 누락 문제가 훅 레벨에서 자동 해결된다. <code class="language-plaintext highlighter-rouge">IntersectionObserverInit</code> 타입을 그대로 훅 파라미터로 노출해두면 threshold/rootMargin도 호출부에서 타입 안전하게 조정 가능하다.</li> <li><strong>reverse() 부수효과</strong>: TS라고 막아주진 않지만, <code class="language-plaintext highlighter-rouge">readonly T[]</code>로 배열을 받는 함수 시그니처를 쓰면 <code class="language-plaintext highlighter-rouge">reverse()</code>/<code class="language-plaintext highlighter-rouge">sort()</code> 호출 자체가 컴파일 에러가 나서 실수로 원본을 건드리는 걸 원천 차단할 수 있다.</li> <li><strong>mask-image</strong>: CSS-in-JS를 쓴다면 벤더 프리픽스 자동 처리(styled-components는 stylis가 처리)를 믿고 표준 속성만 적어도 되는지 먼저 확인. 빌드 파이프라인에 Autoprefixer가 없다면 두 줄을 직접 유지해야 한다.</li> <li><strong>이미지 보호</strong>: <code class="language-plaintext highlighter-rouge">user-select</code>/<code class="language-plaintext highlighter-rouge">user-drag</code>는 전역 CSS 리셋 파일에 한 번만 넣어두면 되는데, DevOps 파이프라인에서 CSP(Content-Security-Policy)나 signed URL 만료 정책 같은 서버 단 보호까지 함께 문서화해두면 “진짜 보호는 어디서 일어나는지” 팀 전체가 헷갈리지 않는다.</li> </ul> </div>]]></content><author><name></name></author><category term="CSS"/><category term="JavaScript"/><category term="Animation"/><category term="Frontend"/><summary type="html"><![CDATA[SOURCE ipad-homepage (fastcampus clone) · STACK vanilla HTML/CSS/JS · 다룰 기법 5가지]]></summary></entry><entry><title type="html">CSS만으로 패럴랙스 배경 만들기</title><link href="https://wonjun-s-dev.github.io/blog/2026/parallax-background/" rel="alternate" type="text/html" title="CSS만으로 패럴랙스 배경 만들기"/><published>2026-07-06T00:00:00+00:00</published><updated>2026-07-06T00:00:00+00:00</updated><id>https://wonjun-s-dev.github.io/blog/2026/parallax-background</id><content type="html" xml:base="https://wonjun-s-dev.github.io/blog/2026/parallax-background/"><![CDATA[<div style="word-break: keep-all; overflow-wrap: break-word;"> <h1 id="css----background-attachment-fixed-">CSS만으로 패럴랙스 배경 만들기: <code class="language-plaintext highlighter-rouge">background-attachment: fixed</code> 파헤치기</h1> <p>JavaScript 라이브러리 없이도 CSS 속성 하나로 그럴듯한 패럴랙스(parallax) 효과를 만들 수 있습니다. 이번 글에서는 실무에서 자주 쓰는 다음 패턴을 뜯어보고, 왜 동작하는지 그리고 언제 문제가 생기는지 정리해봅니다.</p> <div class="language-css highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">.pick-your-favorite</span> <span class="p">{</span>
  <span class="nl">background-image</span><span class="p">:</span> <span class="nf">url</span><span class="p">(</span><span class="s2">'../images/favorite_bg.jpg'</span><span class="p">);</span>
  <span class="nl">background-repeat</span><span class="p">:</span> <span class="nb">no-repeat</span><span class="p">;</span>
  <span class="nl">background-position</span><span class="p">:</span> <span class="nb">center</span><span class="p">;</span>
  <span class="nl">background-attachment</span><span class="p">:</span> <span class="nb">fixed</span><span class="p">;</span>
  <span class="nl">background-size</span><span class="p">:</span> <span class="nb">cover</span><span class="p">;</span>
<span class="p">}</span>

<span class="nc">.pick-your-favorite</span> <span class="nc">.inner</span> <span class="p">{</span>
  <span class="nl">padding</span><span class="p">:</span> <span class="m">110px</span> <span class="m">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div> </div> <h2 id="background-attachment-fixed">핵심은 <code class="language-plaintext highlighter-rouge">background-attachment: fixed</code></h2> <p><code class="language-plaintext highlighter-rouge">background-attachment</code> 속성은 배경 이미지가 스크롤에 반응하는 방식을 결정합니다.</p> <table> <thead> <tr> <th>값</th> <th>동작</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">scroll</code> (기본값)</td> <td>배경 이미지가 요소와 함께 스크롤됨</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">fixed</code></td> <td>배경 이미지가 <strong>뷰포트 기준으로 고정</strong>됨</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">local</code></td> <td>요소 자체의 내부 스크롤에 반응 (내부에 스크롤바가 있는 경우)</td> </tr> </tbody> </table> <p><code class="language-plaintext highlighter-rouge">fixed</code>로 설정하면 배경 이미지는 브라우저 화면에 못박힌 것처럼 고정되고, 그 위의 콘텐츠만 스크롤되며 지나갑니다. 이 “레이어가 분리되어 움직이는” 느낌이 바로 패럴랙스 효과의 정체입니다.</p> <h2 id="section">나머지 속성들의 역할</h2> <div class="language-css highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">background-repeat</span><span class="o">:</span> <span class="nt">no-repeat</span><span class="o">;</span>   <span class="c">/* 이미지 타일링 방지 */</span>
<span class="nt">background-position</span><span class="o">:</span> <span class="nt">center</span><span class="o">;</span>    <span class="c">/* 이미지를 요소 중앙에 배치 */</span>
<span class="nt">background-size</span><span class="o">:</span> <span class="nt">cover</span><span class="o">;</span>         <span class="c">/* 비율 유지하며 요소를 완전히 채움 (넘치면 크롭) */</span>
</code></pre></div> </div> <p><code class="language-plaintext highlighter-rouge">cover</code>와 <code class="language-plaintext highlighter-rouge">fixed</code>를 함께 쓰면, 배경이 뷰포트를 기준으로 그려지기 때문에 화면 크기가 바뀌어도 이미지가 항상 화면을 꽉 채우면서 비율이 깨지지 않습니다.</p> <h2 id="inner-padding-110px-0--"><code class="language-plaintext highlighter-rouge">.inner</code>의 <code class="language-plaintext highlighter-rouge">padding: 110px 0</code>은 왜 필요한가</h2> <p>배경이 <code class="language-plaintext highlighter-rouge">fixed</code>이기 때문에, 실제 이미지 크기와 무관하게 <strong>이 요소의 높이만큼만</strong> 배경이 보이는 “창문” 영역이 결정됩니다. 안에 콘텐츠가 있다면 그 위아래로 여백을 주는 동시에, 섹션 자체의 세로 공간을 확보하는 역할을 합니다. 만약 이 padding이 없고 내부 콘텐츠도 적다면, 섹션 높이가 작아져서 패럴랙스 효과가 거의 안 보이게 됩니다.</p> <h2 id="section-1">실무에서 반드시 알아야 할 함정 3가지</h2> <h3 id="ios-safari---">1. iOS Safari에서 제대로 동작하지 않는다</h3> <p><code class="language-plaintext highlighter-rouge">background-attachment: fixed</code>는 오랫동안 모바일 Safari에서 정상적으로 지원되지 않았습니다. 스크롤 시 배경이 깨지거나, <code class="language-plaintext highlighter-rouge">scroll</code>처럼 그냥 같이 움직여버리는 경우가 많습니다. 실무에서는 보통 미디어 쿼리로 분기 처리합니다.</p> <div class="language-css highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">.pick-your-favorite</span> <span class="p">{</span>
  <span class="nl">background-attachment</span><span class="p">:</span> <span class="nb">fixed</span><span class="p">;</span>
<span class="p">}</span>

<span class="k">@media</span> <span class="p">(</span><span class="n">max-width</span><span class="p">:</span> <span class="m">768px</span><span class="p">)</span> <span class="p">{</span>
  <span class="nc">.pick-your-favorite</span> <span class="p">{</span>
    <span class="nl">background-attachment</span><span class="p">:</span> <span class="nb">scroll</span><span class="p">;</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div> </div> <p>더 확실한 방법은 <code class="language-plaintext highlighter-rouge">position: fixed</code>인 별도의 <code class="language-plaintext highlighter-rouge">div</code>를 배경 전용 레이어로 분리하고, <code class="language-plaintext highlighter-rouge">z-index</code>로 콘텐츠보다 뒤에 배치하는 방식입니다. 이 경우 뷰포트 지원 이슈 없이 동일한 시각 효과를 낼 수 있습니다.</p> <h3 id="section-2">2. 스크롤 성능 이슈</h3> <p><code class="language-plaintext highlighter-rouge">fixed</code> 배경은 스크롤할 때마다 브라우저가 다시 그려야 하는 영역이 늘어나서 리페인트(repaint) 비용이 큽니다. 저사양 기기에서는 스크롤이 버벅이는 원인이 될 수 있습니다. 최근에는 CSS만으로 처리하기보다, <code class="language-plaintext highlighter-rouge">Intersection Observer</code>로 요소가 뷰포트에 들어왔는지 감지하고 <code class="language-plaintext highlighter-rouge">transform: translateY()</code>를 이용해 GPU 가속 애니메이션으로 패럴랙스를 구현하는 방식이 더 널리 쓰입니다.</p> <div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">target</span> <span class="o">=</span> <span class="nb">document</span><span class="p">.</span><span class="nf">querySelector</span><span class="p">(</span><span class="dl">'</span><span class="s1">.parallax-layer</span><span class="dl">'</span><span class="p">);</span>

<span class="kd">const</span> <span class="nx">observer</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">IntersectionObserver</span><span class="p">(</span>
  <span class="p">(</span><span class="nx">entries</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="nx">entries</span><span class="p">.</span><span class="nf">forEach</span><span class="p">((</span><span class="nx">entry</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
      <span class="k">if </span><span class="p">(</span><span class="nx">entry</span><span class="p">.</span><span class="nx">isIntersecting</span><span class="p">)</span> <span class="p">{</span>
        <span class="nb">window</span><span class="p">.</span><span class="nf">addEventListener</span><span class="p">(</span><span class="dl">'</span><span class="s1">scroll</span><span class="dl">'</span><span class="p">,</span> <span class="nx">updateParallax</span><span class="p">);</span>
      <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
        <span class="nb">window</span><span class="p">.</span><span class="nf">removeEventListener</span><span class="p">(</span><span class="dl">'</span><span class="s1">scroll</span><span class="dl">'</span><span class="p">,</span> <span class="nx">updateParallax</span><span class="p">);</span>
      <span class="p">}</span>
    <span class="p">});</span>
  <span class="p">},</span>
  <span class="p">{</span> <span class="na">threshold</span><span class="p">:</span> <span class="mi">0</span> <span class="p">}</span>
<span class="p">);</span>

<span class="nx">observer</span><span class="p">.</span><span class="nf">observe</span><span class="p">(</span><span class="nx">target</span><span class="p">);</span>

<span class="kd">function</span> <span class="nf">updateParallax</span><span class="p">()</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">scrollY</span> <span class="o">=</span> <span class="nb">window</span><span class="p">.</span><span class="nx">scrollY</span><span class="p">;</span>
  <span class="nx">target</span><span class="p">.</span><span class="nx">style</span><span class="p">.</span><span class="nx">transform</span> <span class="o">=</span> <span class="s2">`translateY(</span><span class="p">${</span><span class="nx">scrollY</span> <span class="o">*</span> <span class="mf">0.3</span><span class="p">}</span><span class="s2">px)`</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div> </div> <p><code class="language-plaintext highlighter-rouge">transform</code>은 레이아웃 재계산 없이 GPU 합성 레이어에서 처리되기 때문에, <code class="language-plaintext highlighter-rouge">background-attachment: fixed</code>보다 스크롤 성능이 유리합니다.</p> <h3 id="section-3">3. 겹쳐진 다른 배경과의 쌓임 순서 확인</h3> <p>패럴랙스 섹션 위에 오버레이나 텍스트를 얹는 경우가 많은데, 이때 부모에 <code class="language-plaintext highlighter-rouge">position: relative</code>가 없거나 자식 요소에 <code class="language-plaintext highlighter-rouge">z-index</code>가 없으면 겹침 순서가 꼬일 수 있습니다. <code class="language-plaintext highlighter-rouge">position</code>이 걸린 요소끼리는 stacking context 규칙을 따른다는 걸 기억해두면 디버깅이 빨라집니다.</p> <h2 id="section-4">정리</h2> <table> <thead> <tr> <th>방식</th> <th>장점</th> <th>단점</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">background-attachment: fixed</code></td> <td>코드 3줄로 구현 가능, JS 불필요</td> <td>iOS Safari 이슈, 스크롤 성능 저하</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">position: fixed</code> 배경 레이어</td> <td>모바일 호환성 좋음</td> <td>별도 마크업/CSS 구조 필요</td> </tr> <tr> <td>JS + <code class="language-plaintext highlighter-rouge">transform</code> (Intersection Observer)</td> <td>성능 우수, 세밀한 제어 가능</td> <td>구현 복잡도 증가</td> </tr> </tbody> </table> <p>간단한 히어로 섹션 정도라면 <code class="language-plaintext highlighter-rouge">background-attachment: fixed</code>로 충분하지만, 프로덕션 환경에서 모바일 트래픽 비중이 크다면 처음부터 <code class="language-plaintext highlighter-rouge">transform</code> 기반 JS 패럴랙스를 고려하는 게 유지보수 측면에서 더 안전합니다.</p> </div>]]></content><author><name></name></author><category term="posts"/><category term="학습"/><category term="CSS"/><summary type="html"><![CDATA[background-attachment fixed 파헤치기]]></summary></entry><entry><title type="html">커밋 메시지 컨벤션 가이드</title><link href="https://wonjun-s-dev.github.io/blog/2026/conventional-commits/" rel="alternate" type="text/html" title="커밋 메시지 컨벤션 가이드"/><published>2026-07-02T00:00:00+00:00</published><updated>2026-07-02T00:00:00+00:00</updated><id>https://wonjun-s-dev.github.io/blog/2026/conventional-commits</id><content type="html" xml:base="https://wonjun-s-dev.github.io/blog/2026/conventional-commits/"><![CDATA[<div style="word-break: keep-all; overflow-wrap: break-word;"> <p>협업 프로젝트나 개인 프로젝트 모두에서 일관된 커밋 메시지는 히스토리를 읽기 쉽게 만들고, 자동화된 changelog 생성이나 semantic versioning에도 도움이 됩니다.</p> <p>이번 글에서는 가장 널리 쓰이는 <strong>Conventional Commits</strong> 규칙을 기준으로 용도별 커밋 메시지를 정리해봤습니다.</p> <p><br/></p> <hr/> <p><br/></p> <h3 id="section">기본 형식</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;type&gt;(&lt;scope&gt;): &lt;subject&gt;

&lt;body&gt;

&lt;footer&gt;
</code></pre></div> </div> <p><br/></p> <table> <thead> <tr> <th>요소</th> <th>설명</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">type</code></td> <td>커밋의 성격 (아래 표 참고)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">scope</code></td> <td>변경 범위 (선택, 괄호로 표기)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">subject</code></td> <td>간결한 요약</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">body</code></td> <td>무엇을/왜 변경했는지 상세 설명 (선택)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">footer</code></td> <td>Breaking Change, 이슈 번호 등 (선택)</td> </tr> </tbody> </table> <p><br/></p> <hr/> <p><br/></p> <h3 id="section-1">상세 예시 (전체 커밋 메시지)</h3> <p>실제 커밋 메시지로 전체 예시를 만들어봤습니다.</p> <p><br/></p> <p><strong>예시 1 — 기능 추가 (Breaking Change 포함)</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>feat(auth): 소셜 로그인 기능 추가

기존 이메일/비밀번호 로그인 방식에 더해 Google, Kakao OAuth 로그인을
지원하도록 구현했습니다. 사용자는 이제 소셜 계정으로 회원가입 없이
바로 서비스를 이용할 수 있습니다.

- Passport.js 기반 OAuth2 전략(Google, Kakao) 추가
- 기존 유저 테이블에 provider, providerId 컬럼 추가
- 로그인 성공 시 JWT 발급 로직을 소셜 로그인에도 동일 적용

BREAKING CHANGE: User 스키마의 email 필드가 nullable로 변경됨
(소셜 로그인 유저는 이메일 미제공 가능)

Closes #142
</code></pre></div> </div> <p><br/></p> <p><strong>예시 2 — 버그 수정</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>fix(player): 오디오 시크바 드래그 중 위치 튐 현상 수정

시크바를 드래그하는 동안 currentTime이 매 프레임마다 재계산되면서
드래그 위치와 실제 재생 위치가 어긋나는 문제가 있었습니다.

드래그 중에는 오디오 엘리먼트의 timeupdate 이벤트 리스너를 일시
해제하고, 드래그가 끝난 시점에만 seek을 적용하도록 수정했습니다.

Fixes #98
</code></pre></div> </div> <p><br/></p> <p><strong>예시 3 — 리팩토링 (body만, footer 없음)</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>refactor(store): Zustand 스토어를 도메인별로 분리

기존에 하나의 store.ts 파일에 재생 상태, 유저 상태, UI 상태가
전부 섞여 있어 유지보수가 어려웠습니다.

playerStore, userStore, uiStore로 분리하고, 각 스토어는
자신의 상태와 액션만 관리하도록 구조를 변경했습니다. 기능 변화는
없으며 상태 접근 방식(useStore 훅 사용법)도 기존과 동일합니다.
</code></pre></div> </div> <p><br/></p> <p><strong>예시 4 — 간단한 수정 (subject만, body/footer 생략)</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>chore: .env.example 파일 추가
</code></pre></div> </div> <p><br/></p> <p><strong>요소별로 뜯어보면 (예시 1 기준)</strong></p> <table> <thead> <tr> <th>요소</th> <th>내용</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">type</code></td> <td><code class="language-plaintext highlighter-rouge">feat</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">scope</code></td> <td><code class="language-plaintext highlighter-rouge">auth</code> (괄호 안, 영향 범위를 짧게)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">subject</code></td> <td><code class="language-plaintext highlighter-rouge">소셜 로그인 기능 추가</code> (50자 이내 권장, 마침표 없음)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">body</code></td> <td>빈 줄 하나 띄운 뒤, “무엇을 왜” 서술형 + 필요하면 불릿으로 세부 변경사항 나열</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">footer</code></td> <td>Breaking Change 명시, 이슈/PR 번호 연결 (<code class="language-plaintext highlighter-rouge">Closes #142</code>, <code class="language-plaintext highlighter-rouge">Fixes #98</code>)</td> </tr> </tbody> </table> <p><br/></p> <p><code class="language-plaintext highlighter-rouge">body</code>와 <code class="language-plaintext highlighter-rouge">footer</code>는 선택 사항이라, 예시 4처럼 간단한 변경은 <code class="language-plaintext highlighter-rouge">subject</code> 한 줄로 끝내도 됩니다. 반대로 예시 1처럼 스키마 변경이나 API 변화가 있는 커밋은 <code class="language-plaintext highlighter-rouge">body</code>로 맥락을 남기고 <code class="language-plaintext highlighter-rouge">footer</code>에 <code class="language-plaintext highlighter-rouge">BREAKING CHANGE</code>를 꼭 명시하는 게 나중에 히스토리 추적할 때 훨씬 도움이 됩니다.</p> <p><br/></p> <hr/> <p><br/></p> <h3 id="section-2">타입 한눈에 보기</h3> <table> <thead> <tr> <th>타입</th> <th>용도</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">feat</code></td> <td>새로운 기능 추가</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">fix</code></td> <td>버그 수정</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">refactor</code></td> <td>기능 변화 없이 코드 구조 개선</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">style</code></td> <td>포맷팅 등 로직에 영향 없는 변경</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">docs</code></td> <td>문서 관련 변경</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">test</code></td> <td>테스트 코드 추가/수정</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">chore</code></td> <td>빌드, 패키지 매니저, 설정 등 잡무성 변경</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">ci</code></td> <td>CI/CD 설정 변경</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">build</code></td> <td>빌드 시스템, 외부 의존성 관련</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">perf</code></td> <td>성능 개선</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">revert</code></td> <td>이전 커밋 되돌리기</td> </tr> </tbody> </table> <p><br/></p> <hr/> <p><br/></p> <h3 id="section-3">타입별 상세 예시</h3> <p><br/></p> <h4 id="feat----">feat — 새로운 기능 추가</h4> <p>사용자에게 보이는 새로운 기능이나 화면을 추가했을 때 사용합니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>feat: 사용자 로그인 기능 추가
feat(auth): JWT 토큰 갱신 로직 구현
feat(player): 오디오 시크바 드래그 기능 추가
</code></pre></div> </div> <p><br/></p> <h4 id="fix---">fix — 버그 수정</h4> <p>의도하지 않은 동작이나 오류를 고쳤을 때 사용합니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>fix: MSW가 Range 요청을 가로채는 문제 해결
fix(api): Prisma 쿼리에서 N+1 문제 수정
fix: ARM64/X86_64 아키텍처 불일치로 인한 배포 실패 해결
</code></pre></div> </div> <p><br/></p> <h4 id="refactor-------">refactor — 기능 변화 없이 코드 구조 개선</h4> <p>동작은 그대로 두고 코드 가독성이나 구조만 개선했을 때 사용합니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>refactor: GraphQL resolver 로직 분리
refactor(store): Zustand 스토어 구조 정리
</code></pre></div> </div> <p><br/></p> <h4 id="style-------">style — 포맷팅 등 로직에 영향 없는 변경</h4> <p>코드 로직에는 영향 없는 공백, 세미콜론, 들여쓰기 등을 수정했을 때 사용합니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>style: ESLint 규칙에 따라 들여쓰기 수정
style: 불필요한 공백 제거
</code></pre></div> </div> <p><br/></p> <h4 id="docs----">docs — 문서 관련 변경</h4> <p>README, 주석, API 문서 등 문서만 수정했을 때 사용합니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docs: README에 배포 가이드 추가
docs(api): GraphQL 스키마 주석 보완
</code></pre></div> </div> <p><br/></p> <h4 id="test----">test — 테스트 코드 추가/수정</h4> <p>테스트 코드를 추가하거나 수정했을 때 사용합니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>test: MSW 핸들러 테스트 추가
test(auth): 로그인 실패 케이스 테스트 작성
</code></pre></div> </div> <p><br/></p> <h4 id="chore--------">chore — 빌드, 패키지 매니저, 설정 등 잡무성 변경</h4> <p>소스 코드 로직과 무관한 설정 파일, 패키지 관리 등을 변경했을 때 사용합니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>chore: pnpm workspace 설정 추가
chore(deps): Prisma 버전 업데이트
chore: .env.example 파일 추가
</code></pre></div> </div> <p><br/></p> <h5 id="ci--cicd--">ci — CI/CD 설정 변경</h5> <p>GitHub Actions, Jenkins 등 CI/CD 파이프라인 설정을 변경했을 때 사용합니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ci: GitHub Actions 워크플로우에 ECR 푸시 단계 추가
ci(fargate): 배포 전 헬스체크 스텝 추가
ci: Docker 빌드 캐시 최적화
</code></pre></div> </div> <p><br/></p> <h4 id="build------">build — 빌드 시스템, 외부 의존성 관련</h4> <p>빌드 도구나 외부 의존성 설정을 변경했을 때 사용합니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>build: esbuild 설정에 alias 추가
build(docker): 멀티 스테이지 빌드로 이미지 크기 최적화
</code></pre></div> </div> <p><br/></p> <h4 id="perf---">perf — 성능 개선</h4> <p>속도, 메모리 등 성능을 개선했을 때 사용합니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>perf: 이미지 lazy loading 적용
perf(db): 인덱스 추가로 조회 속도 개선
</code></pre></div> </div> <p><br/></p> <h4 id="revert----">revert — 이전 커밋 되돌리기</h4> <p>이전 커밋을 취소하고 되돌릴 때 사용합니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>revert: "feat: 소셜 로그인 추가" 되돌림
</code></pre></div> </div> <p><br/></p> <hr/> <p><br/></p> <h3 id="section-4">실전 팁</h3> <p><strong>1. scope는 괄호로 표기</strong></p> <p><code class="language-plaintext highlighter-rouge">feat(auth):</code>, <code class="language-plaintext highlighter-rouge">fix(player):</code> 처럼요. 모노레포 구조라면 특히 유용합니다. 예를 들어 <code class="language-plaintext highlighter-rouge">feat(server):</code>, <code class="language-plaintext highlighter-rouge">feat(client):</code>처럼 패키지 단위로 구분할 수 있습니다.</p> <p><br/></p> <p><strong>2. body는 “무엇을/왜” 중심으로</strong></p> <p>제목 아래 빈 줄을 하나 두고 작성합니다. “어떻게”는 코드 자체가 설명해주기 때문에 body에 담을 필요는 없습니다.</p> <p><br/></p> <p><strong>3. Breaking Change 표시</strong></p> <p>타입 뒤에 <code class="language-plaintext highlighter-rouge">!</code>를 붙이거나 본문에 <code class="language-plaintext highlighter-rouge">BREAKING CHANGE:</code>를 명시합니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>feat!: API 응답 구조 변경
</code></pre></div> </div> <p><br/></p> <p><strong>4. 컨벤션 강제하기</strong></p> <p><code class="language-plaintext highlighter-rouge">commitlint</code> + <code class="language-plaintext highlighter-rouge">husky</code> 조합을 추천합니다. pre-commit 훅에서 커밋 메시지 형식을 검사해 규칙에 어긋나면 커밋 자체를 막아줍니다.</p> <p><br/></p> <hr/> <p><br/></p> <h3 id="section-5">마무리</h3> <p>처음에는 번거롭게 느껴질 수 있지만, 몇 번 습관을 들이면 커밋 히스토리만 봐도 프로젝트의 변화 흐름이 한눈에 들어옵니다.</p> <p>특히 CI/CD 파이프라인이나 릴리즈 자동화를 구축할 때는 <code class="language-plaintext highlighter-rouge">type</code>을 기준으로 changelog를 자동 생성할 수도 있어서 데브옵스 워크플로우와도 잘 맞물립니다.</p> </div>]]></content><author><name></name></author><category term="posts"/><category term="학습"/><category term="컨벤션"/><summary type="html"><![CDATA[용도별로 정리한 커밋 메시지 타입과 실전 팁]]></summary></entry></feed>